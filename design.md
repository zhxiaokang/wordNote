# 总览
这是一个微信小程序，初始的repo中是一个简易的微信小程序官方template。本小程序是一个记单词的小程序，但是它自己本身没有单词本，而是靠用户自己平日每当遇到生词之后，查阅词典认识单词后，记录到小程序当中，而后小程序会根据艾宾浩斯遗忘曲线规律，每天提醒用户当天需要复习的单词。

小程序具有3个窗口（窗口的选择卡片出现在屏幕下方）：复习，新词，词汇，我的。

小程序整体风格要符合当代审美（可参考iOS最新系统的设计风格），走简约风，主色调为天蓝色。

## “复习”窗口
进入小程序之后的默认窗口。展示当日需要复习的单词（当然，允许空白，比如新用户尚未添加单词，或者当日没有需要复习的单词，或者当日复习任务已经完成）。

页面主体展示一个单词，下方有两个选项：记得，不记得。无论选哪一个，都会展示出这个单词的释义。
- 如果刚才选了“不记得”，在释义下面出现一个选项：下一个。后面当把今天的所有需要复习的单词都复习了一遍后，这个单词需要再出现。
- 如果刚才选了“记得”，但是有可能用户看了释义后，却发现原来自己记错了。所以在释义下方应该出现两个选项：记对了，记错了。
  - 如果选了“记对了”，那这个单词今天不再需要复习。
  - 如果选了“记错了”，那这个单词就跟上面选了“不记得”的单词进行类似的对待，区别是无需再弹出“下一个”让用户点，而是直接自动跳转到下一个单词即可。

而这个窗口所展示的需要复习的单词，根据艾宾浩斯遗忘曲线规律，组织每天需要复习的单词清单。理想情况下，用户每天都会来这里查看和复习当日需要复习的单词，但是不排除，用户某些天彻底没有来复习，或者是复习了一部分单词就退出了小程序（要在用户当日再次进入小程序时，可以pick up上一次复习进度，继续进行复习，而不是又从头再来），也许后面又进来继续，但是在当日始终没能完成所有复习任务，那没有被复习到的单词就要往第二天顺延。

示例图参考`assets/1*.html`.

## “新词”窗口
用于用户手动记录自己新学习的单词。有三个模块：单词（用户自行手动拼写），词性（这里提供下拉菜单，给出常见的词性，使用缩写，比如名词用 n. 动词用 v. 等等），词意（由于不同词性会有不同词意，所以每当用户选了一个词性之后，后面紧跟着一个可填写的区域，用于用户写词意），词组和例句（用于用户记录相关的词组或例句）。

词性并不是必填项，只要某一行填了词意，这一行就算有效，可以直接保存；用户也可以点“＋添加词性”加多行，分别记录同一个单词不同词性下的词意。词性下拉菜单再次点击已选中的选项可以取消选择。

注意，因为音标字符比较特殊难以添加，所以这里没有音标。

示例图参考`assets/2*.html`.

## “词汇”窗口
这里展示用户所有的单词。在最上方给出三个选择：周，月，年。单词按照被添加的日期组织，根据上面选择的时间跨度折叠和整合到一起。在时间range后面，备注这个范围内的单词数。一些单词数为0的时间range，不用展示，只显示真正有记录生词的时间range. 时间按照由近即远展示。
- 周。比如2026/07/27-2026/08/02（5）.
- 月。比如2026年8月（10）。
- 年。比如2026年（100）。

在时间range前，有一个实心的三角形，用户通过点击它可以折叠和展开本range内的单词表。单词的展示形式是：第一行是单词拼写，第二行是它的词性和词意。点击某单词，可以查看这个单词的详细内容，即当初用户记录的所有关于此单词的内容。

在单词详情页里，用户可以点击“编辑”，对这个单词的拼写、词义（含词性）、词组和例句进行修改，改完点“保存”即可，也可以点“取消”放弃本次修改。编辑只改这几项内容本身，不会影响这个单词的复习进度（stage/下次复习时间）和已经产生的复习记录。复习记录本身是只读的，不能在这里编辑。

旁边还有一个“删除”按钮，点击后弹出确认弹窗，询问用户是否确定删除，确认后这个单词连同它的复习记录一起被永久删除，无法恢复。

示例图参考`assets/3*.html`.

## “我的”窗口
这个小程序不提供账号登录，也不把单词数据上传云端存储——所有单词和复习记录都只保存在用户本机（微信小程序的本地存储）。这样开发者不需要为每个用户的数据同步支付云服务费用，代价是：如果用户卸载微信、清理了微信缓存，或者换新手机时没有把微信数据一并迁移过去，本地数据就可能丢失，小程序本身无法保证这份数据永久不丢。

屏幕从上到下依次是四个模块：

1. **学习打卡**：一个日历视图，当当天的学习任务全都完成（把当日需要复习的单词都复习完，包括选了“不记得”或“记错了”的单词都被再复习并且都标为了“记得”或者“记对了”），给一个笑脸--嘴咧起来；当当日学习任务部分完成（当天复习了，但是没有完全完成任务），给一个面无表情的表情--嘴巴是条横线；当当日虽然有复习任务，但是完全没有复习，根本没有打开过小程序，或者打开了小程序但是干了别的事情，而没有复习单词，给一个哭脸；当当日没有复习任务，这里就不放表情包。
2. **数据备份**：提供“导出数据”和“导入数据”两个按钮。
   - 导出数据：把当前所有单词和打卡记录整理成一段文本，复制到剪贴板，用户可以自己粘贴保存（比如发送到微信文件传输助手、备忘录等），作为手动备份。
   - 导入数据：粘贴此前导出的备份内容，与本机现有数据合并（同一单词或同一天的记录，以更新时间较新的为准，不会用旧备份覆盖新数据）。用于换机后找回数据，或者误删后恢复。
3. **使用说明**：点击后打开一个说明页面，介绍复习/新词/词汇/我的四个窗口每个功能的用法，内容即 `assets/使用说明.md`，两者需保持同步。
4. **用户反馈**：用户可以填写和提交反馈意见给开发者，用户点击进去后，会告诉用户发送邮件给开发者，开发者邮箱是 xkang.zhang@outlook.com

示例图参考`assets/4*.html`.